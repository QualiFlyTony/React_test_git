import React from 'react';
import ReactDOM from 'react-dom';   //this allow us to hook our react application to our index.html

import App from './App'; //the file we create call App
import './index.css';

ReactDOM.render(<App />, document.getElementById('root'));